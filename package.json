{
  "name": "offline-soundslice",
  "version": "1.0.0",
  "description": "Run Soundslice musicsheet locally",
  "main": "index.html",
  "scripts": {
    "server": "python ./api/main.py",
    "client": "npx live-server",
    "start": "concurrently \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "live-server": "^1.2.2"
  }
}
